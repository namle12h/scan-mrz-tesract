services:
  - type: web
    name: scan-mrz-tesract
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn scan_tesract:app --host 0.0.0.0 --port $PORT
    aptPackages:
      - tesseract-ocr